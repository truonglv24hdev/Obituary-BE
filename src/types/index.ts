export enum EGender {
  MALE = "MALE",
  FEMALE = "FEMALE",
}

export enum EPrivacy {
  PUBLIC = "Public",
  PRIVATE = "Private",
  PROTECTED = "Password protected"
}

export enum ERole {
  USER = "USER",
  ADMIN = "ADMIN",
}