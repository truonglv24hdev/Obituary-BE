export enum EGender {
  MALE = "MALE",
  FEMALE = "FEMALE",
}

export enum EPrivacy {
  PUBLIC = "public",
  PRIVATE = "private",
  PROTECTED = "protected"
}

export enum ERole {
  USER = "USER",
  ADMIN = "ADMIN",
}